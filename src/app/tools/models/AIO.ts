export interface AIO {
  id: number;
  device: string;
  brand: string;
  model: string;
  division: string;
  section: string;
  procBrand: string;
  procSeries: string;
  procModifier: string;
  ram: number;
  storage: string;
  storageCap: number;
  screenSize: number;
  videoCard: number;
}
